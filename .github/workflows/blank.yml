name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      with:
        repository: apache/tomcat
        ref: master
        path: "${GITHUB_WORKSPACE}/tomcat"
    - run: ls "${GITHUB_WORKSPACE}/tomcat"
